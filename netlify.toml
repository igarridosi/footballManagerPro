[build]
  command = "npm install && npm run build"
  publish = "frontend/dist"

[build.environment]
  PYTHON_VERSION = "3.11"
  NODE_VERSION = "18.17.0"

[functions]
  directory = "backend"
  node_bundler = "esbuild" 